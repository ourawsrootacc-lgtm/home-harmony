VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional — UI degrades gracefully when blank
VITE_MAPBOX_TOKEN=
# Only used by scripts/seed.mjs (never bundled into the client)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
